/**
 * version of the hub code
 * stamped on the single-file build and its release
 */
const HUB_VERSION = "0.7.0";

/**
 * row layout shared by every tab
 * info row explains the tab, header row names the columns, data starts below
 */
const INFO_ROW = 1;
const HEADER_ROW = 2;
const DATA_ROW = 3;
